name: "sec_insider_pipeline"
version: "1.0.0"
config-version: 2

profile: "sec_insider_pipeline"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  sec_insider_pipeline:
    staging:
      +schema: sec_staging
      +materialized: view
    intermediate:
      +schema: sec_intermediate
      +materialized: view
    marts:
      +schema: sec_marts
      +materialized: table
